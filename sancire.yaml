entrypointDiscovery:
- regex: (?P<type>[^/]+)/(?P<name>[^\.]+).(yaml|json)
  files: true
  context:
    policies:
    - test.test
- regex: cdk/(?P<name>[^\/]+)
  context:
    policies:
    - test.test
